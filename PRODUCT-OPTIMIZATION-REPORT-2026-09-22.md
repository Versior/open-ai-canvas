# 影策产品优化报告：从创意画布升级为 AI 影视生产操作系统

> 日期：2026-09-22  
> 对标产品：FLORA（主对标），Freepik Spaces / Krea Nodes / Figma Weave（交叉验证）  
> 分析范围：当前 `open-ai-canvas` 代码、现有功能文档、Agent 与画布交互，以及公开产品资料。

## 一、执行摘要

### 1. 最重要的判断

当前最值得对标的不是单纯的图片生成器，而是 **FLORA**。它已经把模型调用、无限画布、Agent、可复用工作流、团队协作和外部 API/MCP 组合成完整产品：

- Canvas 支持文本、图片、视频、音频节点，可连接为工作流；
- FAUNA Agent 能读取画布、搭建流程、选择模型、运行任务、整理结果，并提供确认执行和自动执行模式；
- Techniques 可以把一段画布流程封装成可重复运行、分享和派生的“创意应用”；
- 团队拥有实时光标、评论、共享资产、预算与权限；
- API、MCP、CLI 可以从外部工具直接运行工作流。

但影策不应复制成另一个通用 FLORA。更有胜算的定位是：

> **FLORA 帮用户搭建创意工作流；影策帮助用户完成从策划到交付的整部短剧、广告片和影视内容。**

项目当前已经拥有结构化分镜、导演工作台、时间线、批量创作、资产库、多供应商模型、Agent 和自托管能力。这些能力非常适合收敛成一个垂直的 **AI 影视生产操作系统（AI Director OS）**。

### 2. 真正让我从 FLORA 转向影策的五个条件

作为高频创作者，我不会因为“模型更多”或“节点更多”迁移。我会在以下条件同时成立时迁移：

1. **一条完整生产链**：需求、剧本、角色设定、分镜、图像、视频、配音、字幕、时间线、审核和交付都能在一个项目里闭环。
2. **连续性明显更强**：角色、服装、场景、道具、镜头轴线和品牌规范可以被锁定、检查、追踪，而不是每次靠提示词碰运气。
3. **Agent 值得信任**：它确实看到了引用的图片，执行前能给出计划、成本和影响范围，失败后能从断点恢复，而不是只在聊天框里描述它将要做什么。
4. **工作流可沉淀为产品**：做过一次的流程能封装成“应用”，下次只填表单即可运行，团队也能复用。
5. **审核交付效率更高**：客户或团队成员无需学习节点，打开链接即可比较、批注、批准和下载交付包。

这会形成健康的“回不去”：不是靠数据绑架，而是用户积累的角色圣经、品牌规范、Technique、质量标准、审核记录和团队协作关系持续产生复利。

### 3. 具体应新增的产品功能

以下不是方向口号，而是可以直接拆成产品需求和开发任务的功能。

#### A. 画布核心功能

| 功能 | 用户实际操作 | 系统行为 | 优先级 |
|---|---|---|---:|
| 子图保存为应用 | 框选多个节点，点击“保存为应用” | 自动识别输入输出，允许暴露参数，生成版本化的运行表单 | P1 |
| 局部运行 | 右键选择“仅运行此节点 / 从这里向后 / 仅运行已变化节点” | 缓存未变化的上游结果，只重新执行受影响链路 | P1 |
| 流程控制节点 | 添加 If、Switch、Loop、Iterator、Merge、Wait、Approval、Fallback | 支持条件分支、批量迭代、人工检查点、失败降级 | P1 |
| 全局变量节点 | 创建角色、品牌、比例、语言、模型、Seed 等项目变量 | 一个变量修改后自动标记受影响节点，可选择批量重跑 | P1 |
| 强类型端口 | 连接节点时显示输入输出类型 | 不兼容时禁止连接，或自动建议插入转码、抽帧、裁切节点 | P1 |
| Agent 计划预览 | 输入目标后，画布出现半透明“幽灵节点和连线” | 用户可先移动、删改、调整模型，再确认真正写入画布 | P1 |
| 节点执行调试器 | 点击节点的“运行详情” | 展示实际输入、模型参数、提示词、响应、耗时、成本、错误与重试 | P0 |
| 分支实验 | 从任意节点创建 A/B/C 分支 | 并行尝试不同模型、参数、画幅或提示词，结果进入统一比较板 | P1 |
| 胜出结果合并 | 在对比结果中点击“设为主版本” | 下游引用自动切换到胜出版本，失败分支保留但不继续执行 | P1 |
| 画布版本差异 | 选择两个历史版本点击“比较” | 高亮新增、删除、参数变化、连线变化和输出变化 | P2 |
| 场次容器 | 把节点放入“第 3 场 / 夜 / 室内”容器 | 汇总场景、角色、镜头、任务状态、成本和审核进度 | P1 |
| 镜头卡节点 | 创建带镜号、景别、机位、运动、时长、对白的镜头卡 | 与分镜、视频、字幕和时间线保持双向同步 | P1 |
| 断点运行 | 暂停长流程后关闭页面 | 下次进入项目可从最后成功节点继续，不重复消费额度 | P0 |
| 运行预算锁 | 在运行前设置金额、次数或节点上限 | 超限时暂停并请求批准，禁止隐式继续生成 | P0 |

#### B. Agent 助手功能

| 功能 | 具体效果 | 优先级 |
|---|---|---:|
| 视觉上下文回执 | 对话框显示本轮真正传给模型的图片缩略图、顺序、尺寸和引用节点 | P0 |
| 可编辑执行计划 | Agent 先生成步骤列表；用户能删除、重排、换模型和设置检查点 | P0 |
| 计划直接映射画布 | 每个计划步骤对应一个待创建或待运行节点，点击即可在画布定位 | P1 |
| 跨项目资产记忆 | Agent 可以搜索用户授权的历史项目、已批准素材和角色资产 | P1 |
| 先查再改 | 修改节点前自动读取最新版本；画布变化后废弃旧计划并重新确认 | P0 |
| 成本感知模型路由 | 用户选择“最快 / 最省 / 最高质量 / 自定义”，Agent 给出模型组合与价格 | P1 |
| 自动失败恢复 | 模型超时或拒绝时按预设顺序重试、缩小输入或切换备用渠道 | P1 |
| 结果自检 | 生成后自动检查文字、人物、品牌、比例、时长和连续性，不合格时说明原因 | P1 |
| 运行中纠偏 | 用户可以在执行时输入“停止后面的横版，全部改成 9:16” | P1 |
| 操作撤销包 | Agent 一轮产生的多个画布修改可被一次撤销或恢复 | P1 |
| Recipe 调用 | 输入“用我的商品主图广告流程处理这些图片”即可运行已保存应用 | P1 |
| Agent 权限范围 | 分别授权读取、编辑、生成、删除、跨项目搜索和外部调用 | P2 |

#### C. 影视与短剧专属功能

| 功能 | 具体效果 | 优先级 |
|---|---|---:|
| 剧本智能拆解 | 粘贴剧本后生成场次、人物、场景、道具、对白、情绪和镜头建议 | P1 |
| 角色圣经 | 保存角色正侧背、五官、年龄、身材、服装、声音和禁止变化项 | P1 |
| 场景圣经 | 固定时代、空间布局、光照、天气、色彩、道具位置和禁入元素 | P1 |
| 连续性检查器 | 比较相邻镜头的人脸、发型、服装、道具、时间、天气和轴线 | P1 |
| 角色出场追踪 | 显示角色出现在哪些场次和镜头，修改设定时列出受影响资产 | P2 |
| 分镜 Contact Sheet | 按场次集中展示全部镜头候选，支持放大、评分、批准和批量重生成 | P1 |
| 镜头到视频队列 | 批准分镜后自动进入视频生成队列，继承首尾帧、运动和时长 | P1 |
| 首尾帧连续生成 | 上一镜头末帧可以锁定为下一镜头参考，同时保留角色与场景约束 | P1 |
| 对白与口型同步 | 从剧本生成配音，绑定角色音色，并对视频执行口型与时间轴同步 | P2 |
| 字幕轨道 | 自动断句、时间码、说话人、样式、多语言翻译和烧录/外挂导出 | P2 |
| 配乐与音效提示 | 根据场次情绪和镜头节奏生成或推荐音乐、环境音和转场音效 | P2 |
| 交付包 | 一键输出成片、字幕、音轨、分镜表、素材清单、授权记录和项目报告 | P2 |

#### D. 质量、资产与审核功能

| 功能 | 具体效果 | 优先级 |
|---|---|---:|
| 多模型盲评 | 隐藏模型名称，并排比较结果，选择后再揭示成本和模型 | P1 |
| 自动质量评分 | 根据清晰度、构图、文字、人物一致性、品牌规范和可用性评分 | P1 |
| 局部区域批注 | 在图片坐标或视频时间码上直接评论和圈选问题 | P2 |
| 审核状态 | 草稿、待审、需修改、已批准、已拒绝，批准版本禁止被静默覆盖 | P1 |
| 外部审片链接 | 客户不注册也能查看指定版本、评论、批准或拒绝 | P2 |
| 资产血缘图 | 查看素材由哪个输入、模型、提示词、Recipe 和版本生成 | P1 |
| 语义素材搜索 | 使用“穿红衣的女主夜景近景”等自然语言搜索历史素材 | P2 |
| 相似与重复检测 | 自动聚类相似结果，减少图库重复和人工翻找 | P2 |
| 授权与来源字段 | 记录上传来源、模型条款、人物授权、商用范围和过期时间 | P2 |
| 项目质量基线 | 把已批准画面设为项目标准，后续结果自动与其比较 | P1 |

#### E. 界面功能

1. **Build Mode**：完整节点、连线、参数、日志和调试能力。
2. **Run Mode**：只显示输入表单、运行按钮、进度、候选结果和下载，隐藏底层节点。
3. **Review Mode**：Contact Sheet、版本对比、评分、评论和批准集中展示。
4. **Agent 固定输入区**：消息和决策选项独立滚动，输入框永远固定在底部。
5. **右侧 Inspector**：节点只展示关键信息，复杂参数统一进入检查器，避免节点越来越大。
6. **统一任务中心**：集中显示运行中、等待确认、失败、暂停和已完成任务。
7. **成本状态栏**：实时展示本次项目已消费、预计剩余和预算上限。
8. **画布导航面包屑**：进入场次、Recipe 或分组后，可以快速返回项目全景。

#### F. 最先开发的十个功能

1. Agent 视觉上下文回执；
2. 可编辑执行计划；
3. 节点执行调试器；
4. 断点运行与局部重跑；
5. 子图保存为应用；
6. Run Mode；
7. 角色/场景圣经；
8. 连续性检查器；
9. 分镜 Contact Sheet 与多模型盲评；
10. 审核状态与批准版本锁定。

前四项建立可信度和速度，中间两项形成复用，后四项建立影视领域壁垒。它们比继续增加普通生成节点更能直接提高迁移意愿和长期留存。

---

## 二、竞品研究

## 2.1 主对标：FLORA

### FLORA 当前最强的产品结构

| 能力 | 具体效果 | 对高频用户的价值 |
|---|---|---|
| 多模态画布 | 文本、图片、视频、音频节点串联；Element 可复用主体、风格与资产 | 创作上下文不散落在多个工具中 |
| FAUNA Agent | 读取画布、搭建流程、选择模型、批量执行、整理输出；支持确认和自动模式 | 把“节点操作”提升为“目标执行” |
| Techniques | 把工作流与创作上下文封装为可运行、分享、派生的应用 | 用户的经验可以复用并成为团队资产 |
| App Mode | 用简洁表单运行复杂工作流，普通用户不必理解节点 | 同时服务专家和非专家 |
| Quality / Batch | 多版本批量生成、图层编辑、修补扩图、生成历史 | 降低比较与返工成本 |
| 协作 | 实时光标、评论、项目分享、外部协作者使用自己的额度 | 审核不再依赖截图和聊天软件 |
| 开放接口 | API、MCP、CLI 可查询项目、资产、模型并运行 Techniques | 工作流可以进入组织现有系统 |
| 工作区治理 | 共享预算、成员额度、用量可见性、SSO/SAML | 能进入团队和企业采购 |

FLORA 的关键不是某一个模型，而是把“创意过程”变成了可观察、可复用、可协作和可调用的系统。

### FLORA 仍留给影策的机会

1. FLORA 是通用创意工作台，并未围绕影视连续性和整片交付建立强领域模型。
2. FAUNA 当前仍以单 Agent、单项目操作为主，多 Agent 尚不是成熟能力。
3. 通用节点适合探索，但影视用户还需要镜头号、场次、角色连续性、对白、字幕、音轨、审片状态和交付规范。
4. FLORA 的垂直 Studio 思路证明了“隐藏节点、提供任务式入口”是有效路径；影策可以在短剧和广告制作上做得更深。

## 2.2 其他竞品验证出的共识

- **Freepik Spaces / Magnific Spaces**：节点式协作画布、可执行工作流、模板和团队评论已经成为基础预期。
- **Krea Nodes**：多模型并行比较、一键运行整条流程、可分享工作流，对高频生成非常重要。
- **Figma Weave**：复杂节点流可以发布成只有输入控件的 Tool；输入可以隐藏或锁定，底层流程升级时不破坏线上用户。

因此，下一阶段的竞争单位不应是“单个生成按钮”，而应是：

> **可复用生产流程 × Agent 编排 × 质量控制 × 团队审核 × 行业交付。**

## 2.3 国内竞品全景

> 以下比较基于截至 2026-09-22 的官网、官方文档和产品公开页面。未登录或未开放的付费能力只按公开信息判断，不把营销文案当成已经完成的深度体验验证。

国内市场已经不只是“即梦、可灵生成效果好不好”。目前形成了六类竞争者：

1. **Agent 设计画布**：Lovart、星流；
2. **专业视频 Agent 画布**：LibTV；
3. **模型原生创作画布**：可灵灵动画布、即梦智能画布；
4. **工作流与应用生态**：RunningHub、LiblibAI；
5. **影视短剧全流程平台**：天翼云 DramaFlow、MOKI、Filmavo、OOpii；
6. **开源与专业节点工作台**：ToonFlow、NodeLAB 等。

### 国内重点竞品矩阵

| 产品 | 产品形态 | 当前公开的突出功能 | 对影策的实际威胁 | 影策应吸收的能力 |
|---|---|---|---|---|
| Lovart | Agent + 无限画布 | 一句话交付整套设计；30+ 模型自动路由；Brand Kit；Touch Edit；Custom Skills；多格式导出 | Agent 体验和最后一公里编辑明显领先 | Outcome-first Agent、品牌记忆、点选局部修改、技能化工作流、交付格式 |
| LibTV | 面向人类与 Agent 的专业视频画布 | 无限画布、节点工作流、50+ 模型、100+ AI 导演 Skill、剧本/角色/世界观、智能剪辑、工作流与社区、3D-BOX Blender 插件 | 与影策定位最接近，而且已有模型、社区、Skill 和作品案例四层供给 | 人类/Agent 双入口、后端专业创作 Agent、开放 Skill/CLI、作品社区、专业工具插件 |
| 可灵灵动画布 | 视频模型原生画布 | 无限可视空间、智能创作辅助、多人实时协作；与可灵视频、首尾帧、多图参考深度结合 | 模型质量、视频生成和协作入口强 | 多人协作、多参考素材语义绑定、首尾帧与镜头控制统一界面 |
| 即梦智能画布 | 图像编辑画布 | 多图融合、多图层、局部重绘、扩图、消除、抠图；中文理解和视频模型联动 | 普通用户上手快，直接编辑反馈明确 | 圈选即改、图层式编辑、多图融合、低学习成本快捷工具 |
| 天翼云 DramaFlow | 企业级短剧生产平台 | 创意工坊、剧集创作、无限画布；剧本解析、人物场景匹配、资产库、任务中心、团队积分与权限、四层合规 | 对团队和政企市场威胁最大 | 三模式产品入口、企业任务中心、团队预算、素材合规与版权证明 |
| RunningHub / RHTV | 工作流、应用与 API 生态 | ComfyUI 工作流、AI 应用、无限画布、模型 API、工作流 API、创作者收益 | 工作流数量、社区供给和 API 产品化强 | 工作流发布为应用、API 化、模板市场、运行计费和创作者激励 |
| LiblibAI | 模型与工作流社区 | 模型、LoRA、ControlNet、ComfyUI 工作流、训练和社区分发 | 中文模型资产和创作者网络强 | 模型/工作流插件市场、授权信息、作者收益与可复用风格资产 |
| MOKI | AI 短片流水线 | 确认剧本、风格和角色后自动生成分镜、视频素材，再加入音乐、音效和字幕 | 面向非专业用户的完整成片路径清晰 | 一句话到短片的向导模式、自动剪辑、音乐音效字幕闭环 |
| Filmavo | 影视无限画布 | 角色多视角、时空镜头调整、宫格提示词、导演分镜表、3D 导演预览、JSON 风格锁、BYOK、剧本工作室 | 与影策定位高度重合，是最直接的小型产品竞争者之一 | 3D 摄影机预演、结构化风格锁、BYOK 透明账单、剧本阶段导航 |
| OOpii | 本地桌面影视画布 | 本地项目、剧本导入、角色地点道具世界观、智能拆镜、图像视频音频节点、任务队列 | “本地桌面 + 完整影视链路”的表述与影策接近 | 离线/本地优先、工程打包、常见剧本格式导入、声音设计 |
| ToonFlow | 开源短剧 Agent 工作台 | 三层 Agent、持久化记忆、事件图谱、可编程供应商、文件化 Skill | 在 Agent 架构和小说改编上下文上有差异化 | 章节事件图谱、监督 Agent、供应商脚本化、可版本化 Skill |
| openOii | 开源多 Agent 漫剧流水线 | LangGraph 多阶段编排、可恢复/可取消/可反馈运行、WebSocket 进度、tldraw 画布审阅 | 运行状态机和多 Agent 演示链路清晰 | 把生成过程建模为可恢复 Run，而不是一次请求；提供阶段反馈与重入 |
| NodeLAB | 专业节点视觉工作流 | 自带 API、导演/动作/角色节点、镜头版本回放 | 专业用户控制度和成本透明度较强 | BYOK、镜头回放、专业导演节点、供应商解耦 |

### 国内第一直接对标：LibTV

LibTV 在本轮补充研究后，应提升为影策的 **国内第一直接对标产品**。其官网当前公开的数据和能力包括：

- 20+ 独家专业功能；
- 50+ 聚合模型；
- 100+ AI 导演 Skill；
- 10 万+ 创作者社区；
- 剧本、角色、世界观、图片、视频、音频、智能剪辑与导出；
- 工作流沉淀和社区分享；
- 剧本原创与改编、导演执导、角色造型室、创意片头等专用功能；
- 3D-BOX Blender 插件；
- 移动端与网页端数据互通。

它最值得重视的并不是“模型多”，而是 **人类创作者和外部 Agent 共用同一生产引擎**。官方开源的 `libtv-skills` 允许外部 Agent：

1. 创建或切换项目；
2. 上传图片和视频；
3. 用自然语言创建会话；
4. 由 LibTV 后端 Agent 选择模型、编排工作流和生成内容；
5. 查询任务进展；
6. 下载结果并返回画布项目地址。

官方 Skill 特别强调“用户侧 Agent 只负责传话，创作判断交给 LibTV 后端 Agent”。这套设计的产品优点是调用简单、结果统一、Prompt 和模型经验集中更新；缺点是后端决策更黑盒，用户不容易检查它具体使用了哪些输入、提示词、模型和修改步骤。

这正是影策可以反超的位置：

- 同样提供可安装的 Agent Skill、CLI 和 API；
- 但执行前显示可编辑计划、输入引用、模型、成本和画布变更；
- 用户可选择“托管决策”或“完全展开为节点”；
- 每个 Skill 都能查看和 Fork 底层 Recipe；
- 支持平台托管模型，也支持 BYOK、自建模型和私有化部署；
- 后端 Agent 的每一步都进入统一运行证据，而不是只返回最终结果。

### LibTV 与影策的正面对比

| 维度 | LibTV 当前优势 | 影策当前/潜在优势 | 建议 |
|---|---|---|---|
| 画布 | 专业视频创作定位成熟，功能包装统一 | 节点类型、导演台、批量和自托管基础完整 | 不再横向加普通节点，重点提高运行与编辑体验 |
| Agent | 后端专业 Agent + 100+ Skill + 外部 Agent 接入 | 可做成更透明、可编辑、可审计的 Agent | 优先完成视觉输入回执、计划图和运行证据 |
| 模型 | 50+ 模型统一聚合，用户无需配置 | 多供应商、兼容协议、BYOK 和本地模型空间更大 | 做模型能力注册表、健康检查、自动路由与透明成本 |
| 影视流程 | 剧本、角色、世界观、导演、剪辑和作品案例包装成熟 | 已有结构化分镜、项目、时间线和 3D 导演台 | 建立剧集工作室，把已有模块串成一条默认流程 |
| 生态 | Skill、工作流、社区作品、创作者案例 | 开源生态、插件与私有部署可形成不同路线 | 发布官方 Skill SDK、Recipe 市场和插件开发文档 |
| 可控性 | 使用简单，但后端 Agent 决策较黑盒 | 可以让用户检查、修改和复现每一步 | 把“透明且可接管”作为核心卖点 |
| 数据与部署 | 以平台托管体验为主 | 可自托管、可迁移、可控制数据和密钥 | 完整工程包、离线备份和跨部署迁移必须稳定 |

因此，国内竞争的核心目标应调整为：

> **影策不是做一个功能更少的 LibTV，而是做一个可私有化、可审计、可展开、可二开的 LibTV；同时用影视连续性和审核交付把专业深度做得更强。**

### 关键动态：星流正在迁移到 Lovart

星流官网当前公告其将在 2026-10-10 停止服务，并建议用户迁移到 Lovart。这意味着不应再把星流作为长期独立标杆，而应研究其继任产品 Lovart 的 Agent、Brand Kit、Skills、Touch Edit 和导出能力。

这也暴露了一个留存风险：用户不会因为“画布历史很多”就永远留下。影策必须支持完整工程导出、供应商迁移和资产可携带，让用户因为效率和知识沉淀留下，而不是因为无法迁出。

### 开源同源产品扩散带来的风险

公开 GitHub 搜索已经能看到多个基于 `basketikun/infinite-canvas` 或相近代码继续二开的仓库，部分项目增加了工作流表单、插件、多 Agent、导演台或特定供应商接入。对于 AGPL 开源项目而言，普通节点、模型入口和页面样式很容易被其他分支复制。

因此影策不能把护城河建立在“节点数量最多”上，而应建立在：

- 官方版本持续稳定升级和迁移兼容；
- 可量化的 Agent 成功率与回归测试集；
- 影视连续性规则和质量评估数据；
- 高质量 Recipe、Skill、角色与场景资产生态；
- 企业权限、审计、合规和服务能力；
- 插件 SDK、API 与第三方开发者关系；
- 用户信任的品牌和清晰的商业授权。

## 2.4 国内竞品已经验证的功能需求

### 1. 直接在画面上修改，而不是只调节点参数

即梦的局部重绘、消除、抠图、多图融合，Lovart 的 Touch Edit，Filmavo 的涂鸦标注都说明：用户希望点击或圈住错误区域，然后说“只改这里”。

影策应新增 **视觉标注编辑模式**：

- 在图片上画框、涂抹、点选对象；
- 输入修改指令；
- 默认锁定未选区域；
- 生成结果作为新版本并与原图滑杆对比；
- 标注、提示词和 Mask 一起保存，可再次编辑；
- 同一批镜头可以复用相同标注规则。

### 2. 多模态参考需要明确角色，而不是只上传附件

可灵和 Seedance 的竞争力来自多图、视频、音频共同参考。影策不能只提供若干附件槽位，应增加 **参考导演台**：

- 每份参考素材选择用途：角色、服装、场景、构图、动作、运镜、首帧、尾帧、声音、风格；
- 单独设置参考强度和禁止变化项；
- 显示目标模型支持的参考数量和模态；
- 超出上限时由系统给出合并、抽帧或替换建议；
- Agent 必须在计划中说明每份素材会如何使用。

### 3. 短剧产品必须同时提供三种入口

DramaFlow 的“创意工坊 / 剧集创作 / 无限画布”说明一种界面无法覆盖所有用户。影策应形成：

- **快速创作**：模板或一句话生成海报、广告、短视频；
- **剧集工作室**：按剧本、角色、场次、分镜、成片的阶段推进；
- **专业画布**：自由节点、调试、分支和高级模型参数。

三种入口使用同一项目数据，用户可随时互相切换，不能形成三个数据孤岛。

### 4. 工作流必须能够成为可运行商品

RunningHub 已经把工作流和 AI 应用分别提供 API，并通过运行量、收藏和原创度激励创作者。Lovart 也提供可复用 Custom Skills。影策的子图应用需要进一步支持：

- 发布为私有、团队、链接分享或公开应用；
- 自动生成输入表单；
- 显示运行价格、平均耗时、成功率、模型依赖和样例；
- API 调用和网页运行使用同一个版本；
- 作者可更新草稿，但线上版本必须固定；
- 后续开放付费模板、分成和企业内部工作流市场。

### 5. 团队功能不只是实时光标

可灵强调实时协作，DramaFlow 强调子账号、积分分配、权限与消耗追溯。影策应同时实现：

- 多人光标和选区；
- 评论、@成员和任务指派；
- 查看、评论、编辑、运行、审批和管理权限；
- 团队预算和成员额度；
- 每次生成由谁发起、花费多少、使用什么资产；
- 外部客户只能审阅指定版本，不能访问整个工程。

### 6. 国内商用场景必须有合规资产链

DramaFlow 已把提示词改写、出镜人实名、素材校验和成片终审包装成四层合规能力。影策可以更产品化地提供：

- 人物肖像/音色授权附件；
- 模型与 LoRA 商用许可记录；
- 上传素材来源和版权到期时间；
- AI 生成标识策略；
- 成片交付前自动生成版权与素材清单；
- 缺少授权的资产阻止进入“已批准交付包”。

### 7. 小说改编需要事件图谱，不只是长文本上下文

ToonFlow 的章节事件图谱说明，长篇改编不能只把小说全文塞给模型。影策应增加：

- 章节、事件、人物、地点、道具和伏笔实体；
- 事件的时间顺序、因果关系和参与角色；
- 每一场戏追溯到原著事件；
- 改编删减时自动提示后续剧情影响；
- Agent 按当前场次检索必要事实，而不是携带全部文本。

## 2.5 国内竞争下的差异化机会

国内产品已经普遍拥有“无限画布 + 多模型 + 图片视频生成”。这些不再构成独立卖点。影策真正可建立的差异是：

1. **开源、自托管和 BYOK**：项目、密钥、资产与供应商由用户掌控；
2. **可验证 Agent**：展示实际视觉输入、计划、工具调用、成本和画布变更；
3. **影视连续性系统**：角色、服装、场景、道具、声音、轴线与时间连续性自动检查；
4. **工作流产品化**：任意子图可以发布为应用、API 或团队 Skill；
5. **从生成到审片交付**：不是产出若干素材，而是形成已批准、可追责、可交付的版本；
6. **开放连接层**：既可以直连供应商，也能把 RunningHub、ComfyUI、可灵、即梦等作为执行后端；
7. **工程可携带**：项目、资产、Recipe、提示词、运行记录和授权证明可以完整导出与恢复。

## 2.6 国内竞品启发出的新增功能优先级

### P0：必须立即补齐

1. Agent 视觉输入回执与真实看图；
2. 图片圈选、涂抹、局部修改和前后对比；
3. 参考导演台：明确角色/动作/场景/运镜等参考用途；
4. 统一任务中心：排队、运行、失败、取消、重试和实际消费；
5. BYOK 渠道健康检查、自动降级和透明账单。

### P1：形成国内市场差异化

1. 快速创作、剧集工作室、专业画布三模式；
2. 子图发布为应用、表单和 API；
3. 角色/场景/品牌 Kit 与跨项目复用；
4. 影视连续性检查器；
5. Contact Sheet、多模型盲评和批准版本锁；
6. 小说章节事件图谱；
7. 3D 摄影机预演结果回写镜头参数。

### P2：形成商业壁垒

1. 多人实时协作、评论、任务和外部审片；
2. 团队角色、预算、成员额度和审计；
3. 版权授权、肖像/音色许可和合规交付清单；
4. 工作流市场、创作者收益和企业内部模板库；
5. 多 Agent 制作组与监督 Agent；
6. 完整工程包导入、导出、备份和迁移。

---

## 三、影策当前产品判断

## 3.1 已有优势

根据当前仓库，影策并不是从零开始：

- 已有 18 类内置节点，覆盖图片、文本、脚本/分镜、视频、音频、画框、Markdown、SVG、HTML、全景、对比、图表、调色、媒体转换和批量表格；
- Agent 已具备只读、请求确认、自动执行、计划、追问、运行中插话、Skills、记忆和画布读写基础；
- 已有结构化分镜、批量创作表、导演工作台、时间线编辑器、素材库、项目与短剧工作流；
- 已有版本历史、冲突保护和只读公开分享；
- 已有缩略图、命令面板、画布快捷键、空间虚拟化和性能模式；
- 多供应商与自托管能力比完全托管型竞品更适合重视成本、数据和私有化的团队。

这些能力说明：项目不缺“功能点”，真正缺的是把功能点组织成稳定的高频生产闭环。

## 3.2 当前主要差距

| 差距 | 用户感受 | 业务后果 |
|---|---|---|
| 多模态链路仍可能只传元数据而非像素 | Agent 明明配置视觉模型，却说看不到图 | 信任立即下降，后续规划全部不可信 |
| 大量能力停留在节点和面板层 | 新用户需要先理解画布结构 | 首次价值到达慢，学习成本高 |
| 缺少可发布的复用工作流 | 每个项目重复搭节点和提示词 | 经验无法沉淀，团队规模化困难 |
| 缺少一等公民的角色/品牌连续性 | 生成越多，人物和风格越容易漂移 | 影视生产的核心痛点没有被解决 |
| 缺少系统化质量评审 | 用户靠肉眼逐张比较、反复重试 | 模型成本与时间不可控 |
| 分享以只读为主，未形成审片流程 | 批注、批准和版本确认仍在外部工具 | 团队和客户无法留在产品里 |
| 自定义 MCP 尚未开放 | 很难接入组织内部自动化 | 平台效应受限 |
| 大型前端组件承载过多状态 | 面板滚动、布局与复杂交互容易互相影响 | 新功能越多，回归风险越高 |

特别需要注意：当前代码已经具备缩略图、命令面板和画布性能机制，下一阶段不应再次把这些现有能力包装成“新增创新”。

---

## 四、推荐产品战略

### 方案 A：继续做通用 AI 创意画布

- 优点：市场大，功能与竞品容易对齐。
- 缺点：会直接进入 FLORA、Freepik、Krea 和 Figma 的主战场；模型数量与节点数量很难构成壁垒。
- 判断：**不推荐作为主战略。**

### 方案 B：AI 影视生产操作系统

- 核心用户：短剧团队、电商广告团队、内容工作室、导演/分镜师、AI 视频创作者。
- 核心承诺：从 brief 到可交付成片资产，保持人物、品牌、镜头和审核链路一致。
- 判断：**推荐作为主战略。** 这是现有代码能力最自然、最有差异化的收敛方向。

### 方案 C：私有化 AI 工作流编排平台

- 核心用户：有内部模型、素材安全和成本控制要求的企业。
- 优点：自托管、多供应商和开放接口具有优势。
- 判断：可作为方案 B 的企业版，而不是单独做一个抽象平台。

---

## 五、功能优化路线图

## P0：先建立可信赖的生产底座（0—2 周）

### 1. 完成 Agent 真多模态闭环

需要实现的不只是“后端支持图片”，而是完整的用户证据链：

- Agent 输入区显示实际引用的图片缩略图、数量和顺序；
- 消息中显示“本轮模型实际收到的图片”，避免仅有节点元数据；
- 根据所选模型动态校验图片数量、格式、分辨率和总大小；
- 历史图片失效时跳过无关旧引用，并明确提示哪张图不可读取；
- 对真实 OpenAI Chat Completions 兼容渠道做端到端验收；
- 视觉能力检测失败时禁止伪装成“已识别”，给出可操作的修复入口。

**验收标准**：上传商品图并 @ 引用后，Agent 能描述可验证的视觉内容；运行记录能证明图片已进入请求；切换纯文本模型时能在发送前阻止。

### 2. 增加 Run Evidence 执行证据

每次 Agent 或工作流运行都生成可展开的执行记录：

- 输入节点与资产版本；
- 模型、参数、提示词版本；
- 预计/实际成本和耗时；
- 每一步状态、重试次数和错误原因；
- 输出节点及上下游血缘；
- 从失败步骤继续、替换模型重跑、取消剩余步骤。

状态必须真实区分 `planned / queued / running / succeeded / failed / cancelled / skipped`，不能用模糊的“已完成”覆盖部分失败。

### 3. 修复可靠性和并发边界

- 消除模型逻辑名称与实际供应商渠道可能漂移的问题；
- 同轮画布快照刷新必须记录来源，避免并发更新后 Agent 基于旧状态继续执行；
- 给大画布、长对话、长选项、批量任务和大文件建立固定回归用例；
- 为首屏、节点拖拽、Agent 首 token、任务恢复设置性能预算。

P0 完成前，不建议继续堆叠新的模型入口或独立工具按钮。

## P1：形成迁移触发器（3—6 周）

### 1. Recipe / Technique Builder v1

让用户可以框选一段子图并执行：

1. “保存为工作流应用”；
2. 声明输入、输出、变量、默认值与约束；
3. 选择哪些内部节点对运行者隐藏或锁定；
4. 使用样例数据测试；
5. 发布不可变版本；
6. 生成简洁的 Run App 表单；
7. 支持个人、团队和项目内复用；
8. 支持 Fork，并保留来源和版本血缘。

这是粘性的第一核心。用户不再保存一张图，而是在沉淀一条可以重复赚钱或交付的生产线。

### 2. Agent Director Mode

在现有 Agent 基础上升级，而不是再做一个聊天机器人：

- 自动读取用户选中的节点、@ 引用和上传附件；
- 把自然语言目标转换成可编辑的计划图；
- 每一步展示输入、输出、模型、预计耗时和成本；
- 提供 Assist（逐阶段确认）与 Auto（预算和权限范围内自动运行）；
- 支持人类检查点，例如“角色定稿后才能生成全部分镜”；
- 允许并行生成候选分支，再汇合到评审节点；
- 失败后局部恢复，不重跑整条链路。

### 3. Quality Lab

新增专用对比与选择工作区：

- 同一输入并行调用多个模型或参数；
- 隐藏模型名称进行盲评，减少品牌偏见；
- 支持清晰度、人物一致性、构图、文本正确性、品牌符合度等评分维度；
- 自动检查人物脸部、服装、道具、背景、文字、肢体和镜头连续性；
- 胜出版本一键传播到下游节点；
- 自动重试必须有次数和预算上限。

### 4. 角色、品牌与世界观圣经

把它们从普通素材提升为可锁定、可版本化的领域对象：

- 角色：面部、身材、发型、服装、年龄、性格、声音和禁改项；
- 品牌：Logo、色彩、字体、产品外观、语气、禁用表达；
- 世界观：场景、时代、天气、光照、道具和空间关系；
- 每次生成后输出一致性报告和偏差项；
- 下游节点可以固定到某个圣经版本，升级时明确提示影响范围。

这是影策相对通用画布最值得建立的产品壁垒。

## P2：形成高频使用和团队粘性（7—12 周）

### 1. Director Studio

提供默认的任务式体验，而不是一进入就面对无限画布：

`创意需求 → 剧本 → 场次拆解 → 角色/场景资产 → 分镜 → 图片候选 → 视频片段 → 配音/音乐/字幕 → 时间线 → 审核 → 交付包`

每一阶段使用影视语言，展示完成度、阻塞项和下一步。高级用户可以随时点击“在画布中打开”，查看或修改底层节点。

### 2. Batch Matrix

将当前批量表升级为生产矩阵：

- 行：商品、角色、镜头、地区或语言；
- 列：比例、模型、风格、版本、文案；
- 单元格：状态、成本、耗时、评分和批准状态；
- 支持预算上限、并发上限、暂停、断点续跑和失败筛选；
- 输出自动进入 Contact Sheet 评审视图。

### 3. 资产血缘与语义检索

- 支持按画面内容、角色、场景、镜头、项目和生成参数搜索；
- 识别重复或高度相似素材；
- 展示资产从哪个输入、提示词、模型和工作流版本生成；
- 显示它正在被哪些镜头、Technique 和交付包使用；
- 支持智能集合与团队共享集合。

### 4. 审片与批准流程

- 评论固定到节点、画面坐标、时间码和具体版本；
- 状态支持草稿、待审、需修改、已批准、已拒绝；
- 外部客户无需购买席位即可审片；
- 提供版本并排、滑杆对比、批量批准和批准锁定；
- 交付包只允许引用已批准资产。

### 5. Build Mode / Run Mode 双界面

- **Build Mode**：面向专家，完整画布、节点、参数与调试记录；
- **Run Mode**：面向日常操作者，只显示表单、进度、候选结果和审核动作；
- Agent 可停靠或浮动，输入框固定，不随长消息和长选项滚动丢失；
- 节点本体只显示核心状态、耗时、成本和模型，复杂参数收敛到右侧 Inspector；
- 大批量失败按原因分组，不用几十个 Toast 轰炸用户。

## P3：建立平台和网络效应（13—20 周及以后）

### 1. 实时协作

- 实时光标、选区和在线状态；
- 评论、提及、任务指派；
- 查看/评论/编辑/运行权限分离；
- 先完成冲突模型与操作日志，再开放多人同时编辑。

### 2. API / Webhook / MCP / CLI

至少开放：

- 项目、资产、模型、Recipe、运行、审核和交付包；
- 创建运行、查询进度、取消、断点重试；
- 画布读取和受约束的 Changeset 写入；
- 完成、失败、待审核、预算超限 Webhook；
- 外部 Agent 可以发现并运行已发布 Recipe，但必须受工作区权限和预算限制。

### 3. 行业工作流市场

在私有/团队 Recipe 成熟之后，再开放社区市场：

- 分镜转视频、角色定妆、电商广告、多语言改版等垂直模板；
- 评分、运行成功率、平均成本、样例输出和兼容模型透明展示；
- 支持 Fork、版本依赖和创作者收益分成。

### 4. 多 Agent 制作组

不要同时放出多个自由聊天 Agent。应建立共享计划下的明确角色：

- 导演 Agent：镜头意图和整体风格；
- 场记 Agent：人物、服装、道具、轴线和时间连续性；
- 剪辑 Agent：节奏、镜头长度、字幕和音轨；
- QC Agent：格式、品牌、文字、合规和交付检查。

它们只能在职责边界内修改，并通过同一执行记录和审批节点协调。

---

## 六、UI/UX 优化原则

### 1. 默认展示结果和任务，不默认展示系统复杂度

新用户首先看到 Director Studio 或 Run App；只有专家需要进入 Build Mode。画布是底层能力，不应成为每位用户的学习门槛。

### 2. 上下文必须可见

Agent 输入区应直接展示：当前选中节点、@ 引用、图片缩略图、使用的 Recipe 版本、执行权限和预计花费。用户不能靠猜测判断 Agent “看到了什么”。

### 3. 质量比较应成为一级界面

生成结果默认进入网格/Contact Sheet，而不是散落在巨大画布上；支持筛选、放大、盲评、评分、并排比较和胜者传播。

### 4. 进度和错误必须可恢复

长任务应有统一进度抽屉：正在运行、等待审核、失败、可重试、预计剩余时间和已消费成本。错误信息必须定位到步骤并提供下一动作。

### 5. 渐进披露与性能预算

- 首屏只加载可视范围和必要预览；
- 缩放较小时降低节点细节等级；
- 视频和高分辨率图片按需加载；
- 动效以 150—200ms 的状态反馈为主，并支持减少动态效果；
- 键盘操作必须有可见焦点和等价入口。

---

## 七、留存与粘性设计

真正有效的留存循环应是：

1. 用户完成一次项目；
2. 把成功流程保存为 Recipe；
3. 把已批准结果沉淀为角色/品牌资产和质量样本；
4. 下个项目复用，速度更快且一致性更高；
5. 团队在同一流程中批注和批准；
6. 外部系统通过 API/MCP 自动触发；
7. 每次运行继续积累胜出模型、成本和质量数据。

项目用得越久，用户得到的是更准确的工作流和更可靠的交付，而不是一个越来越杂乱的历史图库。

---

## 八、关键指标与验收标准

不要只看生成次数，应追踪：

| 指标 | 目标意义 |
|---|---|
| 首次可用分镜耗时 | 衡量首次价值到达速度 |
| brief 到已批准交付包的周期 | 衡量完整生产效率 |
| Agent 计划一次完成率 | 衡量 Agent 的真实可靠性 |
| 首轮生成接受率 | 衡量输出质量和连续性 |
| 每个已批准资产的平均重试次数 | 衡量成本与返工 |
| Recipe 周复用率 | 衡量流程是否真正沉淀 |
| 使用角色/品牌圣经的项目占比 | 衡量核心壁垒渗透 |
| 预计成本与实际成本偏差 | 衡量预算可信度 |
| 失败任务恢复成功率 | 衡量生产可恢复性 |
| 外部审阅者完成批准的转化率 | 衡量协作闭环 |
| 创作者 D30 与团队席位扩张 | 衡量长期留存和商业价值 |

建议为 P1 设立明确门槛：一个新用户应能在 10 分钟内从模板运行出第一批候选；熟练用户应能把一次成功流程在 3 分钟内发布成可复用应用。

---

## 九、建议实施顺序

| 阶段 | 时间 | 交付重点 |
|---|---:|---|
| 阶段 1 | 0—2 周 | 多模态真链路、运行证据、失败恢复、真实渠道验收、性能回归 |
| 阶段 2 | 3—6 周 | Recipe v1、Run Mode、Agent 计划图、成本与上下文可见 |
| 阶段 3 | 7—12 周 | Quality Lab、角色/品牌圣经、Director Studio MVP、Batch Matrix |
| 阶段 4 | 13—20 周 | 审片流程、资产血缘、API/MCP Beta、团队 Recipe |
| 阶段 5 | 后续 | 实时协作、行业市场、多 Agent 制作组、企业治理 |

周期是产品优先级建议，实际排期应根据团队人数和现有后端数据模型重新估算。

---

## 十、代码落点与架构约束

现有基础可以复用：

- Agent 后端：`D:\影策\open-ai-canvas\backend\internal\app\cloud_agent*.go`
- Agent 前端：`D:\影策\open-ai-canvas\web\src\components\canvas\canvas-cloud-agent-panel.tsx`
- 节点契约：`D:\影策\open-ai-canvas\web\src\types\canvas.ts`
- 画布入口：`D:\影策\open-ai-canvas\web\src\pages\canvas\project.tsx`
- 项目与短剧：`D:\影策\open-ai-canvas\web\src\pages\projects`
- 版本与历史：复用现有项目历史存储和服务。

但不建议继续把新能力堆入大型 Agent 面板和画布页面。应建立有界模块：

- `techniques/`：Recipe 定义、版本、发布、运行；
- `quality/`：评分、比较、规则和自动检查；
- `continuity/`：角色、品牌、世界观和连续性规则；
- `review/`：评论、批准、外部审阅；
- `automation/`：API、Webhook、MCP、任务调度；
- `runs/`：统一运行状态、事件、成本、重试和血缘。

前端组件只负责呈现和交互，运行编排、权限、预算和版本约束必须由服务端强校验。

---

## 十一、明确不要做的事情

1. 不要在 Recipe 和运行系统完成前继续增加孤立的工具按钮。
2. 不要把“支持更多模型”当成核心壁垒；模型会快速同质化。
3. 不要把所有能力隐藏在 Agent 对话里；专业用户需要可编辑、可追踪的结构。
4. 不要先做社区市场，再补版本、质量和权限；顺序应相反。
5. 不要在冲突模型不成熟时直接上线多人实时编辑。
6. 不要复制 FLORA 的品牌和视觉表面；应复制其产品闭环思想，再用影视领域深度超越。
7. 不要用无法解释的积分掩盖成本；尽量同时展示真实货币估算、供应商价格和预算影响。

---

## 十二、最终产品定义

建议把产品的一句话从“开源 AI 无限画布”升级为：

> **影策是一套可私有化的 AI 影视生产操作系统：Agent 将创意目标编排成可审计工作流，角色与品牌圣经保障连续性，团队从分镜到成片在同一个项目中生成、比较、审核和交付。**

如果只能选择三个下一步，应依次完成：

1. **让 Agent 真正看见、理解并可证明它收到的视觉输入；**
2. **让任何成功的子图都能发布为可复用的 Recipe / App；**
3. **用角色/品牌连续性与 Quality Lab 建立通用创意画布难以复制的影视质量壁垒。**

这三项同时解决信任、效率和差异化，是最可能让高频 FLORA 用户真正迁移并长期留下来的组合。

---

## 十三、综合竞品结论与最终产品方案

## 13.1 综合所有竞品后的产品定位

结合 FLORA、LibTV、Lovart、可灵灵动画布、即梦智能画布、RunningHub、DramaFlow、MOKI、Filmavo、OOpii、ToonFlow 和 openOii，影策不应继续定义为“又一个接入很多模型的无限画布”。最终定位应是：

> **开源、可私有化、可审计的 AI 影视生产操作系统。它同时服务人类创作者和外部 Agent，把剧本、角色、场景、分镜、图像、视频、声音、剪辑、审核和交付组织为可复用、可恢复、可协作的生产工程。**

竞争策略不是在每一项都复制竞品，而是完成以下组合：

- 学 FLORA 的 Recipe / App Mode 和协作；
- 学 LibTV 的人类与 Agent 双入口、导演 Skill 和作品生态；
- 学 Lovart 的结果导向 Agent、Brand Kit 和 Touch Edit；
- 学即梦的直接图像编辑；
- 学可灵的多参考视频控制与实时协作；
- 学 RunningHub 的工作流应用、API 和创作者市场；
- 学 DramaFlow 的剧集生产、团队预算和合规；
- 学 MOKI 的“一句话到成片”低门槛流程；
- 学 ToonFlow 的事件图谱和监督 Agent；
- 学 openOii 的可恢复、可取消、可反馈 Run；
- 保留影策自身的开源、自托管、BYOK、多供应商和可二开优势。

## 13.2 最终产品由八个系统组成

### 系统一：三模式统一工作区

同一个项目提供三个入口，但共用一套数据和运行引擎：

1. **Quick Studio**
   - 面向新用户和单次任务；
   - 从模板、Skill 或一句话开始；
   - 只展示必要输入、进度、候选结果和下载。
2. **Production Studio**
   - 面向短剧、广告、漫剧和连续内容；
   - 按剧本、资产、场次、镜头、成片、审核阶段推进；
   - 展示生产进度、阻塞项、预算和交付状态。
3. **Pro Canvas**
   - 面向专业创作者和开发者；
   - 完整节点、连线、参数、分支、调试、日志和 Agent 计划；
   - 可以修改 Quick Studio 与 Production Studio 背后的底层工作流。

### 系统二：专业可审计 Agent

Agent 应同时提供两种使用方式：

- **托管模式**：像 LibTV 一样，用户只描述目标，后端 Agent 完成规划、模型选择和执行；
- **展开模式**：把计划完全展开为画布节点，用户可以检查、修改和接管。

必须具备：

- 实际视觉输入回执；
- 可编辑执行计划；
- 幽灵节点预览；
- 预计成本和耗时；
- Assist / Auto 模式；
- 人工检查点；
- 运行中纠偏；
- 修改前重新读取最新节点；
- 一轮操作整体撤销；
- 自动失败恢复；
- 结果质量自检；
- 跨项目搜索已授权资产；
- 每一步工具、输入、模型、参数和输出可追踪。

### 系统三：专业画布执行引擎

画布不只是摆放素材，需要成为真正的可执行图：

- 强类型输入输出端口；
- 自动插入格式转换节点；
- 全局变量；
- If、Switch、Loop、Iterator、Merge、Wait、Approval、Fallback；
- 仅运行当前节点；
- 从当前节点向后运行；
- 仅运行发生变化的链路；
- 节点缓存；
- 并行分支；
- 预算锁与并发锁；
- 取消、暂停、断点恢复；
- 失败节点局部重试；
- 节点执行调试器；
- 画布版本差异；
- 分支胜出版本合并。

### 系统四：影视领域引擎

这是区别于通用画布的核心：

- 小说与剧本导入：TXT、Markdown、Fountain、FDX、DOCX；
- 章节事件图谱；
- 剧本拆解与改编影响分析；
- 角色圣经；
- 场景与世界观圣经；
- 道具和服装资产；
- 场次容器；
- 专业镜头卡；
- 分镜 Contact Sheet；
- 参考导演台；
- 3D 摄影机预演；
- 首尾帧与镜头延续；
- 对白、配音、口型和字幕；
- 音乐、环境音和音效；
- 时间线与镜头双向同步；
- 多集、多语言、多画幅版本；
- 一键生成完整交付包。

### 系统五：质量与连续性中心

所有生成任务最终进入统一质量系统：

- 多模型并行；
- 隐藏模型名称的盲评；
- 图片和视频并排比较；
- 清晰度、构图、文字、肢体、品牌评分；
- 人脸、发型、服装、场景、道具一致性；
- 时间、天气、灯光、镜头轴线连续性；
- 音色、对白和字幕检查；
- 项目质量基线；
- 不合格原因和局部重生成；
- 已批准版本锁定；
- 胜出结果自动传播到下游。

### 系统六：Recipe、Skill 与应用平台

任意成功子图都能变成复用资产：

- 保存为 Recipe；
- 封装为 Agent Skill；
- 发布为 Run App 表单；
- 暴露为 API / MCP / CLI；
- 定义输入、输出、变量和权限；
- 隐藏或锁定内部节点；
- 测试案例与质量基线；
- 固定线上版本；
- 草稿更新与灰度发布；
- Fork 和来源血缘；
- 个人、团队、链接和公开范围；
- 成功率、耗时、成本和兼容模型展示；
- 后续支持付费模板与创作者分成。

### 系统七：资产、团队和审核

- 角色、场景、道具、品牌、音色和模型资产库；
- 自然语言语义搜索；
- 重复素材检测；
- 生成血缘；
- 素材使用位置；
- 评论固定到图片坐标或视频时间码；
- 草稿、待审、需修改、已批准、已拒绝；
- 外部客户审片链接；
- 多人光标和实时选区；
- 任务指派和 @成员；
- 查看、评论、编辑、运行、审批、管理权限；
- 团队预算、成员额度和消费审计；
- 肖像、音色、模型和素材授权记录；
- 交付前合规与版权清单。

### 系统八：开放生态与本地能力

- BYOK 与平台托管模型并存；
- OpenAI、Anthropic、Gemini 等兼容协议；
- 模型能力注册表；
- 渠道健康检查与自动故障转移；
- ComfyUI / RunningHub 工作流连接器；
- 外部 Agent Gateway；
- Webhook；
- 插件 SDK；
- Blender、Premiere Pro、After Effects、DaVinci Resolve、剪映、Photoshop、Eagle 插件；
- 完整工程包导入导出；
- 本地存储、对象存储和私有云；
- 跨部署迁移与灾难恢复。

## 13.3 用户最终会经历的完整生产流程

理想情况下，用户只需经历这一条主链：

1. 导入小说、剧本、产品 Brief 或参考视频；
2. Agent 创建事件图谱并提取角色、场景、道具和品牌约束；
3. 用户确认角色与场景圣经；
4. Agent 生成可编辑镜头计划和成本预算；
5. 用户确认后批量生成分镜候选；
6. Quality Lab 自动评分并进入 Contact Sheet；
7. 用户批准每个镜头的主版本；
8. 视频节点继承参考导演台、首尾帧和连续性约束；
9. 自动生成配音、音效、音乐和字幕；
10. 镜头进入时间线并完成初剪；
11. 连续性 Agent 和 QC Agent 给出问题列表；
12. 用户或外部客户在时间码上批注和批准；
13. 系统输出成片、字幕、音轨、分镜表、素材清单、授权证明和运行报告；
14. 用户把成功流程保存为自己的导演 Skill，下一个项目直接复用。

这条链路必须允许用户在任意阶段进入 Pro Canvas 检查和接管。

## 13.4 最终开发优先级

### 第一阶段：可信、可运行（P0）

1. 修复 Agent 真多模态输入和视觉回执；
2. 修复长选项、长消息与输入框滚动；
3. 建立统一 Run 状态机和任务中心；
4. 支持取消、失败重试和断点恢复；
5. 增加节点执行调试器；
6. 增加实际成本、耗时和输入输出记录；
7. 增加图片圈选、涂抹和局部修改；
8. 建立模型能力注册表和发送前校验；
9. 建立真实渠道回归测试；
10. 建立大画布、长对话和批量任务性能基线。

### 第二阶段：形成迁移理由（P1）

1. 子图保存为 Recipe / Skill / App；
2. Agent 托管模式与展开模式；
3. 参考导演台；
4. 角色、场景和品牌 Kit；
5. 章节事件图谱；
6. 连续性检查器；
7. Contact Sheet 和多模型盲评；
8. 批准版本锁；
9. Quick Studio / Production Studio / Pro Canvas；
10. 外部 Agent Gateway、MCP 和 CLI。

### 第三阶段：形成团队粘性（P2）

1. 多人实时协作；
2. 评论、任务、外部审片；
3. 团队角色、预算和审计；
4. 资产语义搜索与完整血缘；
5. 合规授权与交付清单；
6. 多集、多语言、多画幅生产矩阵；
7. Blender 和剪辑软件插件；
8. 团队 Recipe 与内部 Skill 市场。

### 第四阶段：形成平台效应（P3）

1. 公开 Recipe / Skill 市场；
2. 创作者主页、作品案例和一键同款；
3. 工作流付费与创作者分成；
4. 多 Agent 制作组；
5. 企业 SSO、审批、私有插件市场；
6. 质量基准数据和模型路由持续学习。

## 13.5 如果资源有限，只做这十二项

1. Agent 真正看图并显示视觉回执；
2. 统一任务中心和可恢复 Run；
3. 节点执行调试器；
4. 圈选/涂抹局部编辑；
5. 参考导演台；
6. 子图发布为 Recipe / Skill / App；
7. Agent 托管与展开双模式；
8. 角色与场景圣经；
9. 影视连续性检查器；
10. Contact Sheet 与批准版本锁；
11. Production Studio 剧集工作流；
12. 外部 Agent Skill、MCP 和 CLI。

这十二项完成后，影策才会同时具备：

- 即梦的直接编辑效率；
- Lovart 的 Agent 使用体验；
- FLORA 的工作流复用能力；
- LibTV 的视频生产与 Agent 生态；
- DramaFlow 的剧集生产结构；
- RunningHub 的应用化能力；
- 以及它们普遍不具备的开源、私有化、BYOK、过程可审计和可完全接管。

## 13.6 最终界面框架

建议主工作区固定为：

- **左侧**：项目、剧集、场次、角色、场景、资产和 Recipe；
- **中间**：Quick Studio、Production Studio、Pro Canvas 或 Review Mode；
- **右侧**：节点 Inspector、角色/场景规则和 Agent；
- **底部**：统一任务中心、运行队列、成本和错误；
- **顶部**：模式切换、版本、协作成员、预算和发布。

Agent 面板内部：

- 顶部显示当前项目、权限和运行模式；
- 中间消息区域独立滚动；
- 计划、问题和审批卡片可折叠；
- 底部输入区固定；
- 输入区上方显示实际引用的节点和媒体缩略图；
- 发送按钮旁显示预计成本和当前模型能力。

## 13.7 最终成功标准

产品达到以下效果时，才具备让用户从竞品迁移后不愿返回的条件：

1. 新用户 10 分钟内得到第一组可用分镜；
2. 成熟用户 3 分钟内把成功流程发布为可复用 Skill；
3. 修改单个镜头不会重新生成整个项目；
4. Agent 的视觉输入、计划、成本和修改始终可验证；
5. 角色和场景连续性问题在生成后自动暴露；
6. 客户能够在一个链接里完成比较、批注和批准；
7. 项目可以自托管、备份、迁移并切换模型供应商；
8. 从 brief 到交付包不需要频繁切换外部工具；
9. 用户积累的角色资产、Recipe、Skill、质量样本和团队流程会持续提高下一个项目的效率；
10. 即使用户导出全部数据，他仍因为产品更快、更稳、更懂影视而选择留下。

---

## 公开资料来源

- [FLORA Canvas](https://flora.ai/product-canvas)
- [FLORA FAUNA Agent 文档](https://docs.flora.ai/editor/fauna)
- [FLORA Techniques](https://flora.ai/product-technique)
- [FLORA Techniques Library](https://flora.ai/techniques)
- [FLORA Collaboration and Sharing](https://docs.flora.ai/editor/collaboration-and-sharing)
- [FLORA Pricing and Workspace Features](https://flora.ai/pricing)
- [FLORA MCP](https://developer.flora.ai/mcp/)
- [FLORA API](https://developer.flora.ai/api/)
- [FLORA Fashion Studio](https://flora.ai/studio/fashion)
- [Magnific / Freepik Spaces](https://www.magnific.com/spaces)
- [Krea Nodes](https://www.krea.ai/features/nodes)
- [Figma Weave Nodes](https://help.weavy.ai/en/articles/12292386-understanding-nodes)
- [Figma Weave Tools](https://help.weavy.ai/en/articles/12267755-tools)
- [Lovart 工作方式与 Skills](https://www.lovart.ai/docs/getting-started/how-lovart-works)
- [Lovart Brand Kit](https://www.lovart.ai/docs/getting-started/things-you-should-set-up-first)
- [Lovart Touch Edit](https://www.lovart.ai/features/touch-edit-ai)
- [Lovart 导出格式](https://www.lovart.ai/docs/export-formats)
- [LibTV 官方网站](https://www.liblib.tv/)
- [LibTV 产品介绍](https://www.liblib.tv/wappro?sourceid=040004)
- [LibTV 官方 Agent Skills](https://github.com/libtv-labs/libtv-skills)
- [星流停止服务及迁移公告](https://www.xingliu.art/)
- [快手：可灵灵动画布官方信息](https://ir.kuaishou.com/zh-hans/news-releases/news-release-details-156)
- [快手：可灵 3.0 系列](https://ir.kuaishou.com/zh-hans/node/11216/pdf)
- [即梦 AI 智能画布](https://www.jimeng.com/)
- [字节 Seedance 2.0](https://seed.bytedance.com/zh/blog/official-launch-of-seedance-2-0)
- [天翼云 DramaFlow 产品介绍](https://www.ctyun.cn/document/11057595/11097099)
- [天翼云 DramaFlow 无限画布](https://www.ctyun.cn/document/11057595/11099043)
- [RunningHub API 产品说明](https://www.runninghub.cn/runninghub-api-doc-cn/)
- [RunningHub 创作者激励](https://www.runninghub.cn/creator-reward)
- [MOKI 官方网站](https://www.moki.cn/)
- [美图：MOKI AI 短片工作流](https://www.meitu.com/zh/media/413)
- [Filmavo 无限画布](https://filmavo.com/)
- [OOpii AI 影像工作台](https://www.oopii.cn/)
- [ToonFlow 开源短剧工作台](https://github.com/Buhaoshawn/ToonFlow)
- [openOii 多 Agent 漫剧生成平台](https://github.com/Xeron2000/openOii)
- [NodeLAB 节点式 AI 视觉工作流](https://nodelab.cn/)
- [basketikun/infinite-canvas 上游开源项目](https://github.com/basketikun/infinite-canvas)
- [OpenAI-X/infinite-canvas 二开版本](https://github.com/OpenAI-X/infinite-canvas)
